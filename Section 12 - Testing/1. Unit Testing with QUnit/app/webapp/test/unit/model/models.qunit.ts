import BindingMode from "sap/ui/model/BindingMode";
import models from "ui5/trippin/model/models";

QUnit.module("Models");

QUnit.test("Should use the correct binding mode for device model", (assert) => {
  // TODO: write the actual test
  assert.expect(0)
  /* const deviceModel = models.createDeviceModel();

  assert.strictEqual(
    deviceModel.getDefaultBindingMode(),
    BindingMode.OneWay,
    "Binding mode is correct"
  ); */
});
